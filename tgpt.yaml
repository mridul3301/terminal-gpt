model: gpt-3.5-turbo  # If you have access to gpt-4 API already, you can update this.
temperature: 0
max_tokens: 500

# Safety: If set to True, commands returned from the AI will prompt the user before running the command.
safety: False
